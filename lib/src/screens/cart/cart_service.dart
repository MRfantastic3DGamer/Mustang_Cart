import 'package:mustang_core/mustang_core.dart';
import 'package:built_value/standard_json_plugin.dart';

import '../../models/cart.dart';

@screenService
abstract class $CartService implements ScreenService {
  // void increment(int i) {
  //   $Cart cart = MustangStore.get<$Cart>() ?? $Cart();
  //   cart = cart.rebuild((b) => b.tiles[i]["count"] = b.tiles[i]["count"] + 1);
  // }
}

// class CartService extends $CartService {}
